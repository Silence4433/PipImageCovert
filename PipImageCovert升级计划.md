# PipImageCovert 统一调色板升级计划

## 一、升级目标

将现有的单图片减色工具扩展为适用于旧版 ImageWorkShop/PIP 资源生产的批处理工具。核心目标是让同一角色、动画或 PIP 所使用的全部 PNG 共用一套调色板，并从工具层面保证整组图片的累计颜色数量不超过指定上限。

升级保持 JDK 1.6 兼容，不覆盖输入素材，输出到用户指定的独立目录。

## 二、第一版：批量共享调色板

第一版解决“逐张减色后，每张图片调色板不同，整组累计颜色仍超过 256”的问题。

计划功能：

1. 从一个目录批量读取 PNG 图片，不递归读取子目录；
2. 汇总整组图片的不透明像素，生成唯一的公共调色板；
3. 总颜色数支持 2～256，默认 248，其中固定预留一个完全透明色；
4. Alpha 阈值可配置，默认 128：低于阈值转为完全透明，其余转为完全不透明；
5. 支持“无抖动”和 Floyd-Steinberg 抖动，无抖动作为旧客户端角色素材的默认选项；
6. 批量输出真正的索引色 PNG，所有输出使用相同的 PLTE/tRNS 调色板及索引顺序；
7. 自动输出标准 768 字节 ACT 调色板；
8. 自动输出 16×16 调色板预览 PNG；
9. 自动输出处理报告，记录输入文件、尺寸、目标颜色数、实际调色板数量、透明阈值及输出位置；
10. 在现有 Swing 主界面增加“批量统一调色板”入口；
11. 保留原有单图打开、缩放、减色和保存功能；
12. 拒绝输入目录与输出目录相同，避免误覆盖原素材。

第一版验收标准：

- 使用 JDK 1.6 语法编译通过；
- 对多张 RGBA PNG 批量处理成功；
- 每张输出均为索引色 PNG；
- 所有输出 PNG 的调色板内容与顺序一致；
- 全部输出图片实际使用颜色的并集不超过目标颜色数；
- 透明像素统一为调色板索引 0；
- ACT 文件长度为 768 字节；
- 不修改、删除或覆盖输入文件。

## 三、第二版：图集、裁剪与 ImageWorkShop 切分文件

第二版建立在第一版公共调色板结果之上，减少人工整理图集和创建 `.s` 文件的工作。

计划功能：

1. 批量裁剪每张 PNG 的透明边界；
2. 保存裁剪前画布尺寸、裁剪矩形和锚点偏移；
3. 生成规则网格 Sprite Sheet，可直接使用 ImageWorkShop 的“快速切分”；
4. 生成紧密排列图集，减少透明空白和图集面积；
5. 为紧密图集生成 ImageWorkShop `ImageDescription.VERSION_4` 兼容的 `.s` 文件；
6. 检查每个图块宽高是否超过合并模式的 255 像素限制；
7. 检查图块数量是否超过 255；
8. 估算合并 PNG 数据规模，并对接近 65535 字节的结果发出风险提示；
9. 输出图块坐标、尺寸、原文件名和锚点偏移清单；
10. 可选按文件名顺序或显式清单控制图块顺序；
11. 提供图集预览和透明边界检查；
12. 保留第一版生成的统一调色板，不在图集阶段重新缩放或重新量化。

第二版不包含自动人体部件识别、遮挡补全或自动生成 CTN 动画。这些工作涉及美术语义和动画图层关系，不应在基础资源转换工具中盲目完成。

## 四、推荐生产流程

```text
AI/已有素材 PNG
  -> 完成尺寸、锚点和造型修正
  -> 第一版批量共享调色板（建议 248 色、无抖动）
  -> 检查输出预览和报告
  -> 第二版裁剪/图集/.s（实现后）
  -> ImageWorkShop 导入或切分
  -> 选择合并256色模式
  -> PIP、CTS、CTN 配套导出
  -> 清理客户端旧缓存并实机验证
```

## 五、兼容与安全约束

- Java 源码保持项目现有编码和 JDK 1.6 兼容语法；
- 不使用 lambda、Stream、try-with-resources或新版集合工厂；
- 输出目录不得等于输入目录；
- 默认不覆盖已存在文件，发生冲突时停止并报告；
- 所有量化应在最终缩放完成后执行；
- 量化后的图片不得再单独缩放、滤镜或调色，否则必须整组重新量化；
- 静态检查和编译通过不等于游戏表现正确，最终必须在 ImageWorkShop 和真实客户端中验证。
