# PipImageCovert 待优化项

更新时间：2026-09-26

## 一、当前状态与未完成边界

当前主要功能已经实现并在工具侧工作正常，但尚未完成客户端真实运行验证。这是当前最重要的未闭环项：离线生成成功、CTS 文件可写出、逐像素重建通过，不等价于客户端真实加载完全正确。

必须补做：

- 在实际 New_ImageWorkShop/Eclipse 环境中完成配置加载、PIP/CTS 保存。
- 在客户端资源链路中加载生成的动画，验证 CTN/CTS、PIP 模式、坐标、透明色、帧序和全部帧显示。
- 记录客户端加载期间的异常、峰值内存、加载耗时和缓存数量。
- 与原版同动画做统一条件下的文件大小和内存对比。

## 二、PIP 数量过多的问题

当前 PipImageCovert 使用 `SAFE_PIM_GROUP_SIZE=32` 作为保守分组上限。该策略的优点是降低单组超出 `pdata/idata` 限制的风险，缺点是可能在容量尚未达到上限时提前创建新的 PIP。因而当前生成很多 PIP，不能直接证明核心算法错误，首先要确认每组实际的：

- 图块数量；
- atlas 数量；
- `pdataLength`；
- 最大 `idataLength`；
- 最终 PIP 文件字节数。

优化方向：在真实编码结果与模拟器一致后，改为容量驱动的装箱：尝试向当前组加入下一个图块，只有数量、颜色、pdata 或 idata 超限时才封组；必要时对候选图块按尺寸、颜色复杂度或编码后大小排序。`32` 应保留为失败保护上限，而不应长期作为主要分组依据。

## 三、动画体积显著大于原版的问题

目前观察到：原版约 37 帧动画总大小约 7-30KB，而新生成约 36 帧动画的多个 PIP 合计约 240KB。这个对比非常重要，但目前还不能仅凭肉眼观察或总文件大小断定是“重复区域算法”单一原因造成。需要拆分测量：

1. 原版与新版本是否使用相同 PIP 模式（PIM/PIP/PJP 等）。
2. 是否使用相同颜色数、透明规则和 PNG 编码方式。
3. 新版本是否把大量小图块分别存入多个 atlas，导致每个 atlas 重复保存调色板/PNG 结构。
4. `base.png` 是否被重复写入多个 PIP（当前代码已修正为只进入第 0 组，但需要客户端实测确认）。
5. 帧差补丁是否是针对共享量化后的结果计算；如果不是，量化误差可能制造大量假差异。
6. 是否存在同像素内容但不同文件名、不同裁剪边界或不同坐标的重复 patch。
7. CTS/PIP 中是否为每个 patch 保留了过多空白边界或过大的切块。
8. 文件大小比较是否把多个 PIP 和 CTS、配置文件按同一口径统计。

## 四、重复区域与帧差算法优化方向

建议按以下顺序验证，避免直接进行不可控的激进修改：

### 4.1 先做统计，不先改算法

构建报告增加：

- 每帧差异像素数、差异包围盒面积；
- 每个 patch 面积、有效像素比例、透明像素比例；
- patch 两两完全相同数量；
- 同内容不同坐标数量；
- 跨帧可复用 patch 数量；
- 量化前后差异像素数；
- 每个 PIP 的实际字节组成。

### 4.2 共享调色板后再计算差异

当前流程已经先生成共享调色板并映射帧，再在映射结果上计算差异，这是正确方向。仍需确认透明阈值、颜色量化和 `dither` 设置在生产流程中固定且一致；生产模式不应启用会制造局部噪声的抖动，除非客户端画质要求明确需要。

### 4.3 跨帧 patch 去重

对完全相同的 patch 内容建立内容哈希或字节签名；相同内容可以只保存一个图块文件，但配置中仍保留不同帧、不同坐标的引用。若同内容图块尺寸不同、透明边界不同或颜色模式不同，不得强行合并。

### 4.4 合并相邻 patch

对同一帧中相邻且间隔很小的 patch，尝试合并后重新计算编码大小；只有合并后的实际 PIP 增量更小才接受。合并不能只看几何面积，因为透明区域和 PNG 过滤结果会改变最终大小。

### 4.5 容量驱动而非固定数量分组

使用真实 PIP 编码后的大小作为装箱依据，优先减少 atlas/PIP 数量，同时保留 65535、255 图块和颜色限制。若真实编码仍无法在 PipImageCovert 独立复现，应把“预测值”和“New_ImageWorkShop 实测值”并列记录，不能盲目放宽分组。

## 五、建议的执行顺序

1. 先完成客户端真实加载测试，确认当前输出没有功能性错误。
2. 增加体积和重复度统计报告，不修改生成结果。
3. 以原版资源为基准，统一模式、颜色、透明和统计口径后重新比较。
4. 优先把固定 32 图块分组改为容量驱动装箱，并用真实 CTS/PIP 生成结果回归。
5. 再实现跨帧完全相同 patch 去重。
6. 最后评估相邻 patch 合并、裁剪策略和更激进的 PNG/调色板压缩。
7. 每一步都必须重新做逐像素重建、CTS 输出、客户端加载和文件大小/内存对比。

## 六、暂不建议的做法

- 不建议直接把 `SAFE_PIM_GROUP_SIZE` 调到 255；这可能重新触发 `pdata/idata > 65535`，并且模拟器与真实编码存在偏差。
- 不建议仅凭肉眼观察认定重复区域过多；应先输出 patch 内容哈希、有效像素率和编码大小统计。
- 不建议先改 PNG 压缩级别；PIP 中真正占体积的可能是 atlas 布局、调色板重复、图块数量和容器结构，而不是 PNG 压缩参数本身。
- 不建议在客户端验证前宣称生产流程已完成。

## 七、当前需要讨论的结论

你的“240KB 对比原版 7-30KB”现象足以说明需要专项优化，但目前更像是多个因素叠加：保守分组导致容器/调色板重复、patch 粒度可能过细、跨帧复用不足，以及两边可能使用了不同 PIP 模式或编码口径。下一步最有价值的不是盲目更激进切分，而是先生成可解释的体积拆解报告，再针对最大项优化。
## 2026-09-26 分组问题第一轮处理结果

已先处理 PIP 数量过多问题，暂不调整重复区域和压缩算法：

- 原先达到 32 个图块就强制新建 PIP，导致安全但偏保守的分组。
- 现改为容量驱动：仅当加入下一个图块后容量模拟失败时才封组。
- 99 个图块的回归样本由 4 组降为 1 组，`pdata=3524`、最大 `idata=4096`，逐像素重建仍通过。
- 这只是工具侧和模拟器侧结果，必须再由 New_ImageWorkShop 生成 CTS/PIP 并进行客户端测试后，才能确认真实 `PipImage.save` 与模拟器边界一致。

因此当前执行顺序调整为：先测试这个减少 PIP 数量的版本；若客户端加载无误，再进入第二阶段的体积优化，包括真实 PIP 文件大小拆解、跨帧 patch 去重、相邻 patch 合并和编码策略比较。
## 2026-09-26 后续执行顺序与处理速度问题

### 后续执行顺序

1. 先验证当前动态容量分组版本生成的 CTS/PIP，并在客户端加载新资源。
2. 客户端确认帧、坐标、透明色、模式和内存均正常后，再把 New_ImageWorkShop 的真实合并核心提取/复制到 PipImageCovert。
3. 将 PipImageCovert 的容量验证改为调用真实核心，完成验证机制一致性测试。
4. 验证机制稳定后，再专门优化 PIP 总体积、跨帧复用、patch 合并和编码策略。

在客户端测试完成前，不改变第二阶段的资源体积算法，避免同时引入分组和编码两个变量。

### 帧差转换有时很慢的初步结论

当前慢速主要发生在“正在检查 PIM 容量”阶段，不应先归因于缓存。`FramePatchComposeBuilder` 对每加入一个图块都会复制当前组并重新调用一次 `PimCapacitySimulator.simulate(...)`；模拟器每次都会重新扫描全部图块、颜色、布局、每个图块索引数据以及 atlas PNG 压缩。图块数量从 99 增加到 303 后，这种重复计算会显著放大，尤其是接近容量上限、需要反复尝试时。

当前实现没有把上一次模拟结果作为持久缓存，也没有读取之前生成目录中的 patch 结果作为缓存；开始转换时还会清理旧的 base、patch 和 compose 输出。因此“同一组图以前很快”更可能由图块数量、图块尺寸/颜色复杂度、当时是否触发容量检查、JVM 热身或系统文件缓存造成，而不是 PipImageCovert 复用了上一次的转换结果。

后续应先记录每个阶段耗时和模拟调用次数，再优化为增量/候选状态复用，或在完成真实核心移植后直接以内存 PIP 编码结果进行更高效的容量检查。优化前不能取消容量验证，以免恢复 PIP 超限问题。
## 2026-09-26 容量检查重复计算优化

已完成低风险优化：第一次分组过程中直接保存每个 patch 的 group 编号，后续写入 `patch.<name>.group` 时直接复用，不再进行第二轮完整 `PimCapacitySimulator.simulate(...)`。

因此当前速度优化状态为：

- 已消除第二轮重复容量模拟；
- 尚未消除第一轮“每加入一个图块就对整个当前前缀重新模拟”的主要开销；
- 下一阶段仍应引入增量容量状态池，并在真实核心移植后用真实 PIP 编码做最终验证；
- 不应为了提速直接关闭容量验证。


@gtalee
1.优化算法，减少帧差转换补丁工作时长----启用旧机制，实现第二点，第一点自动弃用，无需实现
2.移植New_ImageWokShop的核心逻辑到PinImageCovert，对齐双方的pip图片验证机制，当前不对齐，可能出现PinImageCovert通过验证，New_ImageWokShop实际生成时报错！存在风险
3.优化最终生成的pip产物体积，压缩算法或减少重复区域的图块数量
## 2026-09-26 图块状态缓存第一阶段已实施

已按“每个图块只准备一次、保存到 ArrayList、后续组合直接复用”的方案修改：

- 新增 `PreparedPatch` 对象，保存图块宽高、ARGB 像素数组和颜色集合。
- 当前组维护 `ArrayList<PreparedPatch>`；处理第 3 个图块时复用第 1、2 个 PreparedPatch，只准备第 3 个图块。
- 整体 atlas 仍需重新布局和精确 PNG 压缩，因为合并 PNG 不是各图块 PNG 容量的简单相加。
- 当前优化保证判断语义不变，优先消除旧图块重复扫描和重复单图编码；后续如需继续提速，应在真实 PIP 核心统一后优化 atlas 状态增量更新。
## 2026-09-26 合并模式真实产物对照完成

- 已完成 WorkshopPimValidator 与真实 PipImage.save() 的逐组字节特征对照：pdata 长度/CRC、各 atlas 的 idata CRC、atlas 数量均一致。
- 已修复容量预检与生产 CTN 组装的两个关键一致性问题：第 0 组必须包含 base.png 分块；预检必须保持与 PipImage.addFrame 相同的透明索引复用规则。
- 四帧/99 个补丁样本已完成：帧差重建 diffPixels=0；真实 CTN 构建通过；CTS/PIP 回读并逐补丁匹配通过（99/99）。
- 该样本最终分为 2 个 PIP 组：第 0 组 34 个补丁加 base 分块，共 38 个图像帧；第 1 组 65 个补丁。实际 PIP 与预检的 pdata/idata 特征一致。
- PipImageCovert 全部 Java 源码使用 JDK 1.6、-encoding GBK 全量编译通过。
- 客户端真实加载/播放仍未执行，因此尚未交接客户端测试；当前已达到可进行客户端测试的工具链交接条件。

## 2026-09-26 分组性能与真实保存验证进展

- 已将生产分组从逐图块前缀模拟改为指数探测＋二分定位，减少重复的整体 atlas PNG 压缩。
- 已接入完整 PIM 内存保存探针，按 `PipImage.save` 的真实字段顺序构造完整字节流后判断容量；旧 `PimCapacitySimulator` 不再被生产代码调用，仅保留测试引用。
- 四帧／99 图块的重建、真实 CTS 组装和 3 组 PIP 输出均通过。
- 待优化：使用用户实际 304 图块样本做性能基准，并进行客户端加载/播放验证；随后再评估 atlas 增量缓存和最终 PIP 体积优化。

